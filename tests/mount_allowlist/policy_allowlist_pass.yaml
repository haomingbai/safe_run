apiVersion: policy.safe-run.dev/v1alpha1
metadata:
  name: allowlist-pass
runtime:
  command: /bin/echo
  args: ["ok"]
resources:
  cpu:
    max: "100000 100000"
  memory:
    max: 256Mi
network:
  mode: none
mounts:
  - source: /var/lib/safe-run/input
    target: /data/input
    read_only: true
audit:
  level: basic
