apiVersion: policy.safe-run.dev/v1alpha1
metadata:
  name: demo-job
runtime:
  command: /bin/echo
  args: ["hello"]
resources:
  cpu:
    max: "100000"
  memory:
    max: 256Mi
network:
  mode: none
mounts: []
audit:
  level: basic
