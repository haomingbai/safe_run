apiVersion: policy.safe-run.dev/v1alpha1
metadata:
  name: demo-alias
runtime:
  command: /bin/echo
  args: ["ok"]
resources:
  cpu:
    max: "100000 100000"
  memory:
    max: 256Mi
network:
  mode: none
mounts:
  - hostPath: /var/lib/safe-run/input
    guestPath: /data/input
    readOnly: true
audit:
  level: basic
